include lib/_var.pug
include lib/_mixin.pug
<!DOCTYPE html>
html(lang="es")
 include base/_head.pug
  meta(name="title", content=titles[0])
  meta(name="description", content="Nueva página")
  title Nueva página
 body.o-colorIndex

  // footer
  include base/_footer.pug